import React from 'react';
import {Component} from 'react';

export default class Button extends Compoment {
  constructor(props) {
    super(props);
  }

  render () {
    return <button>This is my button</button>;
  }
}